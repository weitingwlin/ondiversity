import numpy as np
import pandas as pd
from sortData import sorting


def grouping(df = None, numGroup = 3, indexGroup = None, groupName = 'Section'):
    
    if df == None:
        df = sorting()
        
    df.assign
    
    if indexGroup == None:
        
    
    
    